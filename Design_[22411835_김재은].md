## 1. Introduction
 인터넷 쇼핑이 발달함에 따라 다양한 쇼핑몰이 생겨나고 있다. 이에 따라 집에서도 비교적 쉽게, 실시간으로 물품들의 정보를 비교할 수 있게 되었다. 또한 쇼핑 사이트에서 어떤 행사를 하느냐에 따라 할인 비율이 천차만별이기에 사이트에 자주 들어가 현재가를 확인하게 되는데 이 과정에서 사용자는 같은 상품 페이지를 반복해 확인해야 한다.<br>
 Price Trcking Secretary는 이러한 불편함을 줄이기 위해 상품 링크와 목표 가격을 등록하면 시스템이 링크에서 가격을 확인해 목표가격 이하가 됐을 시, 알림을 제공하는 서비스이다. 
 
---
  
## 2. Class diagram
<br>
<img width="1230" height="879" alt="image" src="https://github.com/user-attachments/assets/72d21a87-3206-411b-a1d6-0e2311fc6d05" />

[그림 1] class diagram
<br>

### 1) User

| Attributes | Description |
|---|---|
| userId: String | 사용자를 구분하기 위한 고유 ID |
| username: String | 사용자의 로그인용 아이디 |
| password: String | 사용자의 로그인용 비밀번호 |
| email: String | 사용자 이메일 주소 |

| Methods | Description |
|---|---|
| getUserId() | 사용자 고유 ID 가져옴 |
| getUsername() | 사용자의 로그인용 아이디를 가져옴 |
| getEmail() | 사용자의 이메일 주소를 가져옴 |
<br>

### 2) Admin

| Attributes | Description |
|---|---|
| adminId: String | 관리자를 구분하기 위한 고유 ID |
| adminname: String | 관리자의 로그인용 아이디 |
| adpassword: String | 관리자 로그인용 비밀번호 |

| Methods | Description |
|---|---|
| ErrorLog() | 가격 수집 실패 등 오류 기록 확인 |
| SystemLog() | 로그인, 목표 가격 변경 등 시스템 기록 확인 |
| NoticeLog() | 알림 발송 기록 확인 |
<br>

### 3) Product

| Attributes | Description |
|---|---|
| productId: String | 상품 구분 위한 ID |
| PuserId: String | 해당 상품 등록한 사용자의 ID |
| productName: String | 사용자가 등록한 상품명 |
| link: String | 상품 페이지 링크 |
| curPrice: int | 시스템이 가장 최근에 확인한 현재 가격 |
| targetPrice: int | 사용자가 설정한 목표 가격 |
| status: String | 상품의 현재 상태 (품절, 등록 실패 등) |
| addtime: Date | 상품이 등록된 시간 |
| lastCheck: Date | 마지막으로 가격 확인한 시간 |
| nextCheck: Date | 다음 가격 확인 예정 시간 |
| lastNoticePrice: int | 마지막으로 알림을 보낸 가격 |

| Methods | Description |
|---|---|
| updatecurPrice() | 상품의 현재 가격 갱신 |
| changeTargetPrice() | 사용자가 입력한 값으로 목표 가격 변경 |
| updateStatus() | 상품 상태 변경 |
| checkTarget() | 현재 가격이 목표 가격 이하인지 확인 |
<br>

### 4) PriceHistory

| Attributes | Description |
|---|---|
| historyId: String | 가격 이력 기록끼리 구분하기 위한 고유 ID |
| productPriceId: String | 가격 이력과 연결되어 있는 상품 ID |
| price: int | 해당 시점에 저장된 상품 가격 |
| collectedTime: Date | 가격이 수집된 시간 |

| Methods | Description |
|---|---|
| savePrice() | 새로 확인한 가격을 가격 이력에 저장 |
| getPHistory() | 특정 상품의 가격 이력을 가져옴 |
| getTHistory() | 특정 상품 추적 가격의 시간 이력을 가져옴 |
<br>

### 5) Notification

| Attributes | Description |
|---|---|
| notificationId: String | 알림을 구분하기 위한 고유 ID |
| NuserId: String | 알림 받는 사용자 ID |
| productId: String | 알림 발생 상품 ID |
| productName: String | 알림 발생한 상품명 |
| currentPrice: int | 알림 발생 당시의 상품 현재 가격 |
| targetPrice: int | 사용자가 설정한 목표 가격 |
| message: String | 사용자에게 보여줄 알림 메시지 |
| createTime: Date | 알림이 생성된 시간 |
| sendNotice: String | 알림 전달 방식 |
| isRead: Boolean | 사용자가 알림을 읽었는지 여부 |

| Methods | Description |
|---|---|
| markRead() | 알림을 읽음 상태로 변경 |
| markUnread() | 알림을 안 읽음 상태로 변경 |
<br>

### 6) ErrorLog

| Attributes | Description |
|---|---|
| errorid: String | 오류 기록을 구분하기 위한 고유 ID |
| productid: String | 오류가 발생한 상품 ID |
| errorType: String | 오류 종류 |
| message: String | 오류 내용을 설명하는 메시지 |
| createTime: Date | 오류 발생한 시간 |

| Methods | Description |
|---|---|
| savelog() | 발생한 오류 내용을 로그로 저장 |
| getLog() | 저장된 오류 로그 가져옴 |
<br>

### 7) Login

| Attributes | Description |
|---|---|
| inputid: String | 사용자가 입력한 아이디 |
| inputPd: String | 사용자가 입력한 비밀번호 |

| Methods | Description |
|---|---|
| checkUser() | 입력한 아이디와 비밀번호가 저장된 사용자 정보와 일치하는지 확인 |
| login() | 로그인 성공 시 메인 화면으로 이동 |
| logout() | 현재 로그인 상태를 해제한다 |
| returnLoginResult() | 로그인 성공 또는 실패 결과를 사용자에게 공지 |
<br>

### 8) ProductRegister

| Attributes | Description |
|---|---|
| productName: String | 사용자가 입력한 상품명 |
| productLink: String | 사용자가 입력한 상품 페이지 주소 |
| targetPrice: int | 사용자가 입력한 목표 가격 |

| Methods | Description |
|---|---|
| checkInput() | 상품명, 상품 링크, 목표 가격이 올바르게 입력되었는지 확인 |
| addProduct() | 입력된 상품 정보를 바탕으로 새로운 상품 등록 |
| showRegister() | 상품 등록 성공 또는 실패 결과를 사용자에게 보여줌 |
<br>

### 9) ProductManager

| Attributes | Description |
|---|---|
| productList: List | 사용자가 등록한 상품 목록 |

| Methods | Description |
|---|---|
| showProductDetail() | 상품의 상세 정보를 보여줌 |
| showProductList() | 사용자가 등록한 관심 상품 목록을 보여줌 |
| removeProduct() | 사용자가 선택한 상품을 목록에서 삭제 |
| changeTargetPrice() | 사용자가 선택한 상품의 목표 가격 변경 |
<br>

### 10) PriceTracker

| Attributes | Description |
|---|---|
| checkTime: Date | 가격을 확인한 시간 |
| collectedPrice: int | 새로 확인한 상품 가격 |
| checkResult: String | 가격 확인 결과 |

| Methods | Description |
|---|---|
| checkPrice() | 상품의 현재 가격 확인 |
| mockPriceData() | 모의 가격 데이터 생성 |
| savePriceHistory() | 확인한 가격을 가격 이력에 저장 |
<br>

### 11) Scheduler

| Attributes | Description |
|---|---|
| checkInterval: int | 가격 확인 시간 간격 |
| nextCheckTime: Date | 다음 가격 확인 예정 시간 |

| Methods | Description |
|---|---|
| startScheduler() | 스케줄러 실행 |
| runPriceTracking() | 정해진 시간이 되었을 때 가격 추적 기능 실행 |
| runNoticeCheck() | 목표 가격 도달 여부 확인 후 알림 처리 기능 실행 |
| updateNextCheckTime() | 가격 확인 후 다음 확인 시간 갱신 |
<br>

### 12) NotificationManager

| Attributes | Description |
|---|---|
| notificationList: ArrayList<Notification> | 생성된 알림 목록 |

| Methods | Description |
|---|---|
| checkTargetPrice() | 현재 가격이 목표 가격 이하인지 비교 |
| checkDuplicateNotice() | 같은 가격에서 이미 알림이 보내졌는지 확인 |
| createNotice() | 목표 가격에 도달했을 때 새 알림 생성 |
| sendNotice() | 웹페이지에서 확인할 수 있는 알림 전송 |
| sendEmailNotice() | 이메일 알림 전송 |
| markAllAsRead() | 사용자의 알림을 모두 읽음 상태로 변경 |
<br>

---
## 3. Sequence diagram

### 1) Login
<img width="1064" height="852" alt="image" src="https://github.com/user-attachments/assets/4b6fcd50-b99e-4de4-9d25-54db57f09c52" />
<br>

시스템 사용자가 서비스에 로그인 하는 과정이다. 사용자가 아이디와 비밀번호를 입력하면 Login 클래스에서 checkUser(inputid, inputPd)를 통해 입력된 정보가 시스템에 저장된 정보와 동일한지 확인한다.
<br>
1. 아이디나 비밀번호가 입력되지 않은 경우, returnLoginResult("입력값 누락") 을 실행해 입력값이 누락되었다는 메세지를 출력한다.
2. 입력 정보가 일반 사용자의 정보와 일치하는 경우, User 클래스에서 사용자 정보를 확인하고 login을 실행해 메인 화면으로 이동한다.
3. 입력 정보가 관리자와 일치하는 경우, Admin 클래스에서 관리자 정보를 확인하고 login을 실행해 관리자 로그 화면으로 이동한다.
4. 입력 아이디, 비밀번호가 저장되어 있는 정보와 일치하지 않는 경우, returnLoginResult("로그인 실패")를 실행하고 로그인 실패 메세지를 출력한다. 
<br>
<br>

### 2) 상품 등록
<img width="758" height="919" alt="image" src="https://github.com/user-attachments/assets/e13af17c-856a-47d7-b3d0-2dfc2c34b340" />
<br>

사용자가 상품을 등록하는 과정이다. <br>
사용자가 상품명, 링크, 목표 가격을 입력하면 productRegister 클래스는 checkInput(productName, productLink, targetPrice)를 통해 입력값이 제대로 되어 있는지 확인한다.
<br>
1. 입력값이 올바른 경우, ProductRegister는 Product 클래스에 addProduct(productName, productLink, targetPrice)를 요청해 새로운 상품 정보를 저장한다. 정보를 저장하면 ProductRegister는 showRegister("상품 등록 완료")를 실행하고 사용자에게 상품 등록 완료 메세지를 출력한다.
2. 상품명, 상품 링크, 목표 가격 중 하나라도 입력되지 않은 경우, ProductRegister가 showRegister("입력값 누락")을 실행 후 입력값 누락 메세지를 출력한다.
3. 상품 링크 형식이 올바르지 않은 경우, showRegister("링크 형식 오류")를 실행하고 링크 형식 오류 메시지를 출력한다.
4. 목표 가격 형식이 올바르지 않은 경우에는 showRegister("가격 입력 오류")를 실행하고 가격 입력 오류 메시지를 출력한다.
5. 입력을 제대로 진행하였으나 정보를 저장하는데 실패한 경우, ProductRegister는 ErrorLog 클래스에 savelog("상품 저장 실패")를 요청해 오류 내용을 저장하고 showRegister("상품 등록 실패")를 실행해 사용자에게 상품 등록 실패 메세지를 출력한다.
<br>
<br>

### 3) 관심 상품 목록 조회
<img width="1222" height="816" alt="image" src="https://github.com/user-attachments/assets/7d8c1965-3e9a-4cbf-a1f8-705b9c3135fa" />
<br>

사용자가 관심 상품 목록을 조회하는 과정이다. <br>
사용자가 관심 상품 목록 조회를 요청하면 ProductManager 클래스는 showProductList(uwerId)를 실행하여 현재 로그인한 사용자의 상품 목록을 불러온다. 이후 Product 클래스에 사용자 id와 연결된 상품 목록을 요청한다.
<br>
1. 등록된 상품이 있는 경우, Product는 상품 목록을 ProductManager에 반환하고 ProductManager는 사용자에게 관신 상품 목록을 출력한다.
2. 등록된 상품이 없는 경우, 빈 상품 목록을 반환하고 사용자에게 등록된 상품이 없다는 메세지를 출력한다.
3. 상품 목록을 불러오는 과정에서 문제가 생기는 경우, ProductManager는 ErrorLog 클래스에 savelog("상품 목록 조회 실패")를 요청해 오류 내용을 저장하고 사용자에게 상품 목록 조회 실패 메세지를 출력한다.
<br>
<br>

### 4) 가격 추적
<img width="893" height="793" alt="image" src="https://github.com/user-attachments/assets/db49c260-086e-4ece-ade2-9af4d6513509" />

<br>

가격 추적 기능은 사용자가 실행하는 기능이 아닌 Schedule가 정해진 시간마다 자동으로 실행하는 기능이다.<br>
Scheduler는 startScheduler(checkInterval)를 써서 가격 확인 주기를 관리하고, 정해진 시간이 되면 PriceTracker에 runPriceTracking(productList)를 요청한다. 이후 PriceTracker는 checkPrice(productId)를 실행해 등록된 상품의 현재 가격을 확인한다.
<br>

1. 가격 확인에 성공한 경우, PriceTracker은 mockPriceData(productId)를 통해 모의 가격 데이터를 생성하고 Product 클래스에 updatecurPrice(productId, curPrice)를 요청해 상품의 현재 가격을 갱신한 다. 이 다음 PriceTracker savePriceHistory(productPriceId, price, collectedTime)를 실행하고, PriceHistory 클래스에 savePrice(productPriceId, price, collectedTime)를 요청해 가격 이력을 저장한다. 이후 Product 클래스의 updateStatus(productId, status)를 실행해 상품 상태를 갱신한다.
2. 가격 확인에 실패한 경우, PriceTracker는 Product 클래스에 updateStatus(productId, "수집 실패")를 요청해 상품 상태를 수집 실패로 변경 후 Errorlog 클래스에 savelog("가격 확인 실패")를 요청해 오류 내용을 저장한다.
3. 가격 처리가 끝나면 Scheduler는 updateNextChecktime(nextCheckTime)을 실행해 다음 가격 확인 예정 시간을 갱신한다.
<br>
<br>

### 5) 가격 추이 조회
<img width="1125" height="872" alt="image" src="https://github.com/user-attachments/assets/51df6ff7-a719-4a83-b64a-cdec4efbbf2f" />
<br>

가격 추이 조회 기능은 사용자가 등록한 상품의 가격 변동 흐름을 확인하는 과정으로 사용자가 상품 상세 화면에 접속했을 때 ProductManager 클래스가 showProductDetail(productPriceid)를 요청해 해당 상품의 가격 이력을 불러온다. 그리고 Product 클래스에 상품 정보 요청을 보내서 상품명, 현재 가격, 목표 가격 등의 정보를 가져온다.
<br>

1. 가격 이력이 있는 경우, ProductManager는 PriceHistory 클래스에 getPHistory(productPriceId)를 요청해 해당 상품의 가격 이력을 가져온다. 이후 getTHistory(productPriceId, collectedTime)를 통해 시간 기준으로 정리된 가격 이력을 받아오고, 사용자에게 상품 상세 정보와 가격 추이 그래프를 출력한다.
2. 가격 이력이 없는 경우, PriceHistory는 빈 가격 이력을 반환하고 ProductManager는 사용자에게 가격 이력 없음 메시지를 출력한다.
3. 가격 이력 조회에 실패한 경우, ProductManager는 ErrorLog 클래스에 savelog("가격 이력 조회 실패")를 요청해 오류 내용을 저장한다. 이후 사용자에게 가격 이력 조회 실패 메시지를 출력한다. <br>

---
## 4. State machine diagram
---
## 5. Implementation requirements  

---

## 6. Glossary  

---

## 7. References
